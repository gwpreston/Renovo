{% extends 'layout.twig' %}

{% block title %}{{ t('auth.link_expired') }} — {{ instance_name }}{% endblock %}

{% block content %}
    <section class="card narrow">
        <h1>{{ t('auth_verify_failed.title') }}</h1>
        <p class="muted">{{ t('auth_verify_failed.intro') }}</p>
        <p><a class="button" href="/login">{{ t('action.back_to_sign_in') }}</a></p>
    </section>
{% endblock %}
