{% extends 'layout.twig' %}

{% block title %}{{ t('auth.link_expired') }} — {{ instance_name }}{% endblock %}

{% block content %}
    <section class="card narrow">
        <h1>{{ t('auth_reset_expired.title') }}</h1>
        <p class="muted">{{ t('auth_reset_expired.intro') }}</p>
        <p><a class="button" href="/forgot-password">{{ t('auth_reset_expired.request_a_new_link') }}</a></p>
    </section>
{% endblock %}
