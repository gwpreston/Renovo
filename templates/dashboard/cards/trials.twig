{% import 'partials/logo.twig' as logo %}
{# Free trials about to start charging — the card worth seeing first. #}
{% if stats.trials is not empty %}
    <section class="card callout">
        <h2>{{ t('dashboard.trials_ending_soon') }}</h2>
        <p class="muted">{{ t('dashboard.trials_note') }}</p>
        <ul class="renewals">
            {% for trial in stats.trials %}
                <li>
                    <span class="renewal-name">
                        {{ logo.mark(trial, 20) }}
                        <a href="/subscriptions/{{ trial.id }}/money">{{ trial.name }}</a>
                    </span>
                    <span class="renewal-date">
                        {{ t('dashboard.converts_on', {'date': trial.trialEndDate|local_date('d MMM')}) }}
                    </span>
                    <span class="renewal-amount">
                        {{ trial.priceAfterConversion.amountMinor|money(
                            trial.priceAfterConversion.currency) }}
                    </span>
                </li>
            {% endfor %}
        </ul>
        <p class="muted">
            {% for total in stats.trial_totals %}
                {{ total.total_minor|money(total.currency) }}{{ not loop.last ? ' · ' : '' }}
            {% endfor %}
            {{ t('dashboard.trials_total_note') }}
        </p>
    </section>
{% endif %}
