{# The same yearly figure, expressed per day, week, month and year. #}
<section class="stat-grid">
    {% for label, amount in stats.per_period %}
        <article class="card stat stat-small">
            <h2 class="stat-label">{{ t('period.per_' ~ label) }}</h2>
            <p class="stat-value">
                {{ amount is not null ? amount|money(stats.combined_yearly.currency) : '—' }}
            </p>
        </article>
    {% endfor %}
</section>
