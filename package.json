{
  "name": "renovo-assets",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end build for Renovo. Compiles Tailwind, bundles the JavaScript and vendors the webfont into public/build, so a running instance loads nothing from a third-party host.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "chart.js": "^4.5.0",
    "lucide": "^0.544.0",
    "tailwindcss": "^4.1.13"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.13",
    "vite": "^7.1.5"
  }
}
